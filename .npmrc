# @n8n/node-cli pins a strict eslint peer (e.g. eslint@9.29.0) that conflicts
# with the eslint version resolved in the tree, so a plain `npm install`/`npm ci`
# fails with ERESOLVE. Until the upstream peer range is loosened, legacy peer
# resolution is required to install this package's toolchain.
legacy-peer-deps=true
